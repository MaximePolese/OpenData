<Window x:Class="WpfApplication1.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="Metro API" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Column="0" Grid.Row="0" Content="Longitude" Margin="25" VerticalAlignment="Top" HorizontalAlignment="left" />
        <TextBox Grid.Column="1" Grid.Row="0" Height="25" Text="{Binding Lon}" Width="100" Margin="25" VerticalAlignment="Top"
                 HorizontalAlignment="left" />
        
        <Label Grid.Column="0" Grid.Row="1" Content="Latitude" Margin="25" VerticalAlignment="Top" HorizontalAlignment="left" />
        <TextBox Grid.Column="1" Grid.Row="1" Height="25" Width="100" Text="{Binding Lat}" Margin="25" VerticalAlignment="Top"
                 HorizontalAlignment="left"/>
        
        <Label Grid.Column="0" Grid.Row="2" Content="Distance" Margin="25" VerticalAlignment="Top" HorizontalAlignment="left" />
        <TextBox Grid.Column="1" Grid.Row="2" Height="25" Width="100" Text="{Binding Dist}" Margin="25" VerticalAlignment="Top"
                 HorizontalAlignment="left" />
        
        <Button Grid.Column="2" Grid.Row="2" Height="25" Command="{Binding Search}" Content="Search" Width="100"
                VerticalContentAlignment="Center" Padding="5" Margin="25" VerticalAlignment="top"
                HorizontalAlignment="left"/>
        
        <ListView Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3" Name="LinesList" ItemsSource="{Binding Lines}" Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding id}" Header="Id" />
                    <GridViewColumn DisplayMemberBinding="{Binding name}" Header="Name" />
                    <GridViewColumn DisplayMemberBinding="{Binding lon}" Header="Longitude" />
                    <GridViewColumn DisplayMemberBinding="{Binding lat}" Header="Latitude" />
                    <GridViewColumn DisplayMemberBinding="{Binding zone}" Header="Zone" />
                    <GridViewColumn DisplayMemberBinding="{Binding lines}" Header="Lines" />
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Window>
